# Media Downloader

一个基于 React Native（Expo）的跨平台媒体抓取与下载工具。它内置一个「浏览器式」的 `WebView`，像 Chrome 扩展的内容脚本（Content Script）一样注入采集逻辑，从任意网页中嗅探、解析并下载图片与视频资源。

## 功能特性

### 1. 浏览器式采集（无自动化特征）
- 内置可见的 `WebView`，模拟真实桌面浏览器（Desktop UA）加载页面，支持完整的 JS 渲染与重定向流程。
- 注入 **Stealth 脚本**隐藏 `navigator.webdriver`、伪造 `plugins` / `languages` / `vendor` 等特征，规避站点反爬检测。
- 注入 **静音脚本**，自动静音页面音视频与 WebAudio，自动播放不吵人。
- 支持手动干预：前进、后退、刷新、手动重新抓取、停止抓取。

### 2. 多层视频嗅探「四层漏斗」
注入到页面内的采集脚本按证据强度由高到低组织，最终汇入同一候选池：
1. **DOM 层**：`<video>` / `<source>` / `<embed>` / `<object>` 节点（最可信，但通常只有当前播放的那条）。
2. **结构化数据**：JSON-LD、`og:video`、`og:image`、`<link rel="preload">` 等站点主动声明的地址。
3. **网络层**：通过 `PerformanceObserver` / `fetch` / `XHR` 钩子，覆盖懒加载与无扩展名的 CDN 直链，并读取接口响应体（JSON / HLS 清单）。
4. **数据层**：遍历接口 JSON 与页面内嵌 `STATE`（如 `window.__playinfo__`），翻出藏在响应体里的直链。

靠「证据」判定而非域名白名单，理论上适用于任意站点。

### 3. 站点适配层（B 站 DASH 音画分离）
- 专门适配 B 站：解析 `window.__playinfo__` / `window.__INITIAL_STATE__` 的 DASH 结构，将视频轨与全部伴音轨配对（按带宽兜底），解决「下载得到无声 m4s」问题。
- 自动等待 B 站播放器延迟初始化，确保音轨地址就绪。

### 4. 媒体解析与可播放性校验
- 对 HLS（`m3u8`）、DASH（`mpd` / `m4s`）、渐进式（`mp4` / `webm`）进行容器嗅探与传输形态判定。
- 在 WebView 内试播直链，判断是否为登录态 / 防盗链资源。
- 通过 `HEAD`（降级 `Range`）并发探测文件体积、清晰度、时长等元数据。

### 5. 下载与保存
- 支持单条与批量下载，批量时实时显示进度与成功/失败统计。
- 三种落地方式：系统相册（Gallery）、分享面板（Share）、文件系统（File）。
- **纯 JS 的 fragmented MP4 合并器**：针对 DASH 音画分离资源（如 B 站），流式合并视频轨与伴音轨为单文件，无需外部依赖。

### 6. 浏览与筛选
- 图片 / 视频结果列表，支持关键词筛选与按体积 / 分辨率 / 时长 / 标题排序。
- 视频内置播放器，失败时可回退到主播放列表解析出的最佳清晰度 / 备用 CDN 直链。

## 技术栈

| 类别 | 选型 |
| --- | --- |
| 框架 | React Native + Expo（SDK 53） |
| 内核 | expo / react-native 0.79 |
| WebView | react-native-webview |
| 文件系统 | expo-file-system / expo-media-library |
| 分享 | expo-sharing |
| 图标 | @expo/vector-icons |
| 构建 | EAS Build（`eas.json`） |

## 测试安装教程

### 环境要求
- **Node.js**：建议 18 LTS 或更高版本
- **npm** 或 **yarn** 包管理器
- **Expo CLI**：`npm install -g expo-cli`
- 一台用于测试的手机：
  - **iOS**：需安装 [Expo Go](https://expo.dev/go)（App Store），或使用 EAS Build 构建 dev 客户端
  - **Android**：需安装 [Expo Go](https://expo.dev/go)（Google Play / APK），或开启开发者模式 + USB 调试

### 步骤一：克隆并安装依赖
```bash
git clone <your-repo-url> media-downloader
cd media-downloader
npm install          # 或 yarn install
```

### 步骤二：启动开发服务器
```bash
npm start            # 等价于 npx expo start
```
启动后会显示一个二维码（QR code）与若干选项：
- 按 `a` 在已连接的 Android 设备 / 模拟器上打开
- 按 `i` 在 iOS 模拟器上打开
- 用手机上的 **Expo Go** 扫描终端里的二维码直接运行

### 步骤三：在手机上运行（最常用）
1. 手机与电脑处于同一局域网。
2. 手机打开 Expo Go，点击「Scan QR code」。
3. 扫描终端二维码，应用即会加载到手机上。

### 步骤四：构建独立测试包（可选，更接近生产）
使用 EAS Build 生成可安装的 dev / preview 包：
```bash
# 首次使用需登录 Expo 账号
npx eas login

# 构建 Android APK（preview 配置）
npx eas build -p android --profile preview

# 构建 iOS 预览包（需 Apple 开发者账号）
npx eas build -p ios --profile preview
```
构建完成后下载安装到设备即可离线测试。

### 步骤五：验证核心功能
1. 在应用内输入一个含图片/视频的网页地址（如 B 站视频页、图文帖等）。
2. 等待页面加载与自动采集，确认结果列表出现图片与视频卡片。
3. 点击某条视频，确认能正常试播（B 站应带声音）。
4. 勾选若干资源，点击下载，确认能保存到相册 / 文件系统 / 分享面板。

## 已知限制
- 通过 **MSE**（MediaSource，播放 `blob:` 源）或 **WebRTC**（`srcObject` 直播流）的站点拿不到可下载直链，仅能诊断并提示。
- HLS / DASH 分片流暂不支持逐分片下载合并，仅支持音画分离的 fMP4 合并（DASH）。
- 部分站点的强登录态 / 复杂风控（如抖音异常重定向）可能无法稳定采集。

## 目录结构（核心）

```
src/
├── App.tsx                 # 应用入口与导航
├── components/             # UI 组件（WebView、结果列表、筛选、播放器等）
│   └── BrowserStyleWebView.tsx   # 浏览器式采集 WebView（含 Stealth / 静音脚本）
├── services/               # 业务逻辑
│   ├── extractor.ts        # 注入页面的采集脚本（四层漏斗 + B 站适配）
│   ├── scrape.ts           # 抓取流程编排
│   ├── downloader.ts       # 下载与保存（相册/分享/文件）
│   ├── videoProbe.ts       # 视频可播放性校验与播放兜底
│   ├── sizeProbe.ts        # 文件体积并发探测
│   ├── mp4Muxer.ts         # 纯 JS fMP4 音画合并
│   └── media.ts            # 媒体列表归一化与筛选
├── types.ts                # 类型定义
└── constants.ts            # 时间配置、数量上限、UA 等
```

## 许可证
见 `LICENSE`。
